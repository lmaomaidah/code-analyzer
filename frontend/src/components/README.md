# components/

This folder will hold reusable React components built in Week 5.

Planned:
- `ScoreGauge.jsx`     — circular gauge showing the 0-100 quality score
- `IssueList.jsx`      — filterable, sortable list of detected issues
- `SeverityChart.jsx`  — Chart.js bar chart of issues by severity
- `ReportButton.jsx`   — triggers PDF download
